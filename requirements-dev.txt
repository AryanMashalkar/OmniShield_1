# OmniShield dev / test dependencies (not required at runtime).
# Install with:  venv\Scripts\pip install -r requirements-dev.txt
-r requirements.txt

pytest==9.1.1
httpx==0.28.1   # required by starlette.testclient.TestClient
