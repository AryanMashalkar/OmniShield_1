# Copy to `frontend/.env`. Must match OMNISHIELD_API_KEY on the backend.
# `.env` is gitignored. Any key shipped to a browser is readable in devtools —
# this is a dev/demo convenience, not production auth.
VITE_OMNISHIELD_API_KEY=change-me-to-a-long-random-string
